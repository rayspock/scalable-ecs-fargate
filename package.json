{
  "name": "scalable-ecs-fargate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "docker-compose up --build -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["AWS", "ALB", "ECS", "Fargate", "ServiceDiscovery", "CloudFormation"],
  "author": "ray",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}
